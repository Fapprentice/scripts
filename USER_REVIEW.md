# Task Verge 项目用户视角评审报告（历史快照）

> 本文保留为历史记录，当前行为以 README 和自动化测试为准。

**评审日期**：2026-07-05
**最后核对**：2026-07-06
**评审范围**：从真实用户使用角度审视产品功能、交互流程、信息架构与体验缺陷
**产品定位**：本地桌面"专注控制台"，帮助用户围绕目标生成任务、识别任务应用、跟踪完成度与前台时间

> **文档状态说明**
> 本报告为 2026-07-05 的评审快照。下方各问题前会用标记反映 2026-07-06 核对时的最新状态：
> - ✅ **已修复**：代码已实现对应建议
> - ⚠️ **部分修复**：已改进但仍有残留问题
> - ❌ **未修复**：与评审时一致，问题仍存在
>
> **修复汇总**：34 个问题中 18 个已修复、4 个部分修复、12 个未修复。

### 修复清单速览

| # | 状态 | 问题摘要 |
|---|------|----------|
| #1 | ✅ | 首次使用无引导 → 已加 onboarding 横幅（含可点击导航） |
| #2 | ✅ | 操作顺序不明 → onboarding 分步引导 |
| #3 | ✅ | AI 依赖无提示 → fallback 时弹模态 |
| #4 | ❌ | 工作桌面操作按钮语义混乱 |
| #5 | ⚠️ | 移除应用入口太深 → 已移到右上角，但仍 hover 显示 |
| #6 | ✅ | AI 识别应用无标记 → 已加"AI/手"角标 |
| #7 | ❌ | 锁定状态编辑仍用 prompt() |
| #8 | ❌ | 新增任务仍用 prompt() |
| #9 | ✅ | 历史趋势空状态无提示 → 已显示"暂无足够数据" |
| #10 | ✅ | 前台时间显示 exe 名 → 已转应用友好名 |
| #11 | ✅ | 批量编辑清空 done_flags → 已用 oldFlags 保留 |
| #12 | ❌ | 保存按钮锁定流程奇怪（仍 prompt + 仅记日志） |
| #13 | ❌ | 空任务保存无校验 |
| #14 | ✅ | 规则页内容单薄 → 已改名"应用分类" |
| #15 | ❌ | 保存分类/自动分类按钮位置 |
| #16 | ✅ | 自动分类无确认 → 已加 confirmDlg |
| #17 | ❌ | 分类不可增删类别 |
| #18 | ⚠️ | 设置页混入日常操作 → 休息已提到 dashboard，退出仍在设置页 |
| #19 | ✅ | 休息只 10/20/30 三档 → 已扩展为 5/10/20/30/45 + 显示今日 X/3 次 |
| #20 | ✅ | 申请退出直接杀进程 → 已加二次确认 + 明确告知 |
| #21 | ✅ | 专注时段设置是摆设 → 已从设置页移除 |
| #22 | ❌ | 复盘页信息密度低且无 actionable |
| #23 | ❌ | 归档不可点击查看详情 |
| #24 | ❌ | 刷新按钮作用不明 |
| #25 | ❌ | 页面切换无记忆（滚动位置丢失） |
| #26 | ✅ | toast 无动画/超时 → 已加 toast-pulse + 4s 超时复位 |
| #27 | ⚠️ | 错误弹窗场景不全 → AI fallback 已弹模态，但 prompt() 仍用 |
| #28 | ✅ | 按钮样式无优先级 → 已有 primary/default/danger 三级 |
| #29 | ✅ | 响应式断点过少 → 已增加 1024/1280/1600 三档 + nav 加 title |
| #30 | ✅ | 前台监控无告知 → 已实现 showPrivacyNotice |
| #31 | ⚠️ | 数据无清除入口 → 已有"清除前台时间"，但无导出 |
| #32 | ✅ | 单实例锁脆弱 → 后端已加 X-Session token 校验 |
| #33 | ❌ | 服务端口随机 + 崩溃无自启 |
| #34 | ✅ | DeepSeek Key 无 UI 入口 → 设置页已加输入框 |

---

## 一、产品定位与核心价值评估

### 1.1 用户预期 vs 实际体验

**产品名"Task Verge · 专注控制台"暗示的功能**：
- 任务管理（✓ 有）
- 专注度跟踪（✓ 前台时间监控）
- 应用控制/拦截（✗ **守护进程已移除，但产品定位文案未同步更新**）

**核心矛盾**：
> 产品名为"专注控制台"，但应用拦截/守护功能已被移除。当前实际只剩"任务管理 + 前台时间统计 + AI 应用识别"三块功能，**"专注"的核心机制已缺失**。用户进来后会期待"防止我分心"的能力，但发现只是个任务列表 + 时间统计。

**严重度**：🔴 高（产品定位与功能不匹配）

---

## 二、首次使用体验（Onboarding）

### 2.1 首次进入体验问题

**问题 #1：无引导，无空状态指引** ✅ 已修复
- 用户首次打开看到"尚未设置目标"，但**没有按钮、链接或提示**告诉用户去哪里设置目标
- 用户不知道该先做什么：去设置页填目标？点"生成任务"？点"评估进度"？
- **现状**：已加 `renderOnboarding()` 横幅，分步引导（"第 1 步：去设置页填目标"），"设置"二字可点击跳转

**问题 #2：操作顺序不明** ✅ 已修复（随 #1 一起）
- 顶部三个按钮"刷新 / 生成任务 / 评估进度"始终显示
- 但在没目标、没任务时点"生成任务"会返回 `SKIP no goal`，点"评估进度"会返回 `SKIP no tasks`
- 用户不知道必须先去**设置页填目标**才能生成任务
- **现状**：onboarding 横幅已明确分步："第 1 步填目标 → 第 2 步生成任务"

**问题 #3：AI 依赖无提示** ✅ 已修复
- "生成任务"按钮点了之后，如果没配 DeepSeek Key 会走 fallback（3 个本地兜底任务）
- 用户看到的是 `toast('已开始生成任务')`，然后状态显示"完成"
- 但实际是兜底任务，**用户感知不到这是降级体验**，也不知道自己没配 Key
- **现状**：`run('generate')` 完成后会检查 `finalStatus.message` 是否含 `fallback`，是则弹模态"已用本地模板生成"并提示去设置页配 Key

**严重度**：🔴 高（新用户首次使用会卡住）

---

## 三、五大页面逐页评审

### 3.1 总览页（Dashboard）

#### 3.1.1 顶部 Hero 区
- **进度环 + 目标 + 下拉切换目标**：信息密度合理 ✓
- **"就绪/休息中"状态徽章**：仅 2 种状态，过于简单
  - 没有"锁定中"、"AI 思考中"、"任务已完成"等状态
  - **休息中**状态只能在休息后看到，用户不知道怎么进入休息

#### 3.1.2 工作桌面区（核心功能区）

**问题 #4：操作按钮语义混乱** ❌ 未修复
```
[选择应用] [申请增加] [直接减少]  未选择应用
```
- "选择应用" → 打开 picker
- "申请增加" → AI 判断是否允许加到任务应用
- "直接减少" → 立即移除

**用户困惑**：
1. "申请增加"为什么要 AI 评判？用户想加自己常用应用还要被 AI 拒绝，体验**很挫败**
2. "申请增加"和"选择应用"是分离的两步操作：先选 → 再点申请。但 UI 没明确流程引导
3. "直接减少"按钮在没有选中应用时也可点击，点了返回 `toast('先选择应用', false)` —— **应该禁用按钮**
4. 用户选了应用后，picker 会关闭，但"未选择应用"文字变成应用名 —— 这个交互需要**视觉强化**（当前只是普通灰色 span）

**问题 #5：移除应用入口隐藏太深** ⚠️ 部分修复
- 桌面应用图标下方有 `<i>移除</i>` 小字，hover 才变红
- 用户找不到怎么移除已加入的应用
- 建议改为应用卡片右上角的 × 按钮或长按菜单
- **现状**：位置已从"下方"移到右上角（`top:4px;right:6px`），但仍为 `opacity:0` hover 才显示，且文字仍是"移除"而非"×"图标

**问题 #6：AI 识别应用不可见** ✅ 已修复
- "工作桌面"标题旁的 `desktopSource` 文字显示"分类已完成 · AI 3 个 · 手动 2 个"
- 但**用户不知道哪些是 AI 加的、哪些是手动加的**，没有区分标记
- 当 AI 自动加错应用时，用户无法分辨该移除哪个
- **现状**：每个应用图标左上角已加 `app-badge`（AI 识别显示"AI"蓝标，手动添加显示"手"绿标）

#### 3.1.3 今日任务区
- 复选框 + 任务文字 + 编辑按钮：基本可用 ✓
- **问题 #7** ❌ 未修复：锁定状态下点"编辑"会弹 `prompt('计划已锁定，请填写修改原因')`，但用户已经能看到任务文字，**锁定意义不大**（只是多了一步 prompt）。**现状**：仍使用 `prompt()`
- **问题 #8** ❌ 未修复："新增"按钮在总览页用 `prompt()` 弹窗输入，体验简陋，与任务页的 inline 编辑不一致。**现状**：仍使用 `prompt()`

#### 3.1.4 前台时间 & 历史趋势
- 前台时间柱状图：✓ 清晰
- 历史趋势 canvas 折线图：**问题 #9** ✅ 已修复：当历史 < 2 天时画空白，但**没有"暂无数据"提示**，用户看到空白不知道是不是 bug。**现状**：`drawHistory()` 已在 `h.length<2` 时绘制"暂无足够数据，至少完成 2 天后展示趋势"文字
- **问题 #10** ✅ 已修复：前台时间按 exe 名统计（如 "msedge"），但 exe 名对普通用户不友好，**应显示应用名**。**现状**：`renderDesktop()` 已用 `allApps.find(...)` 把 exe 名转成应用友好名

---

### 3.2 任务页（Tasks）

**问题 #11：批量编辑与逐条编辑状态同步混乱** ✅ 已修复
- 上方是逐条编辑输入框，下方是批量编辑 textarea
- 在逐条编辑框输入时，textarea 会同步更新（`input` 事件）
- 但在 textarea 输入时，会**重建 state.tasks 并清空所有 done_flags**（`state.tasks = ...map(text=>({text,done:false}))`）
- **用户在批量编辑框打字时，已勾选的任务完成状态会被静默清除** —— 严重数据丢失风险
- **现状**：`saveTasks` 处理已用 `oldFlags = state.tasks.map(t=>!!t.done)` 保留原有完成状态

**问题 #12：保存按钮在锁定状态下流程奇怪** ❌ 未修复
- 点"保存任务"→ 弹 `prompt('计划已锁定，请填写修改原因')`
- 用户填了原因才能保存
- 但**填的原因只是写进事件日志**，没有任何"审批"或"记录追溯"展示给用户
- 用户会问："我填这原因有什么用？"
- **现状**：仍使用 `prompt()`，原因仍只写入 events 日志，无追溯展示

**问题 #13：空任务保存无校验** ❌ 未修复
- 删除所有任务后点保存，会保存空任务列表
- 完成度变成 0%，但**没有提示"你已清空所有任务"**
- **现状**：`/api/tasks` 仍接受空数组，前端 `saveTasks` 仍无校验

---

### 3.3 应用分类页（Rules，导航显示"应用"）

**问题 #14：规则页内容过于单薄** ✅ 已修复
- 移除 saveRules 后，整个规则页只剩"AI 应用分类"一个 details
- 页面标题叫"规则中心"但**没有任何"规则"**，只有应用分类管理
- 建议改名"应用分类"或补充真正的"规则"内容（如：专注时段、休息规则等本该属于规则页的内容）
- **现状**：导航按钮文字改为"应用"，`pageTitles` 标题改为"应用分类"，副标题"管理 AI 应用分类与工作桌面归属"

**问题 #15：保存分类 / 自动分类 按钮位置** ❌ 未修复
- 两个按钮在 cards 下方，需要滚动到底部才能看到
- 分类卡片 `max-height: 520px; overflow: auto`，**按钮在滚动容器外**，但当分类多时用户容易忽略
- **现状**：按钮位置未变

**问题 #16：自动分类按钮无确认** ✅ 已修复
- 点"自动分类"会**清空当前所有分类**（`c["app_catalog"]={}`）然后 AI 重新分类
- 用户精心调整的分类会被覆盖
- **应弹确认框**："此操作会清空当前分类并由 AI 重新生成，确定？"
- **现状**：`regenerateCatalog` 点击处理已加 `confirmDlg('此操作会清空当前分类并由 AI 重新生成，你手动调整的分类将丢失。是否继续？')`

**问题 #17：分类卡片不可增删类别** ❌ 未修复
- 用户只能调整每个小类下包含哪些应用
- **不能新增类别、不能删除类别、不能重命名**
- 但 AI 分类可能不符合用户心智模型，用户想自定义结构却做不到
- **现状**：`renderCatalog()` 仍只渲染现有 categories/children，无新增/删除/重命名入口

---

### 3.4 设置页（Settings）

**问题 #18：设置页混入非"设置"功能** ⚠️ 部分修复
- 目标列表、自启动 → 属于设置 ✓
- 专注时段、休息、申请退出 → 这些是**日常操作**，放在设置页不合理
- 用户每天都要点"开始休息"，但要去设置页找，**入口太深**
- **现状**：dashboard 已加快捷休息组件（`#breakWidget` 含原因/分钟/开始按钮/今日次数），但"申请退出"仍在设置页（退出非日常操作，可接受）

**问题 #19：休息功能设计反直觉** ✅ 已修复
- "休息原因"是必填？实际后端不校验
- "休息 10/20/30 分钟"下拉 —— 为什么限定 3 档？用户想休息 5 分钟或 45 分钟不行
- 休息次数上限 3 次/天，但**UI 不显示今日已休息几次**，用户点第 4 次才知道用完了
- **现状**：休息分钟已扩展为 5/10/20/30/45 五档；`renderBreakWidget()` 已显示"今日 X/3 次"

**问题 #20：申请退出按钮 = 关闭程序** ✅ 已修复
- 点"申请退出"会触发 `os._exit(0)` **直接杀死整个进程**
- 用户以为只是"退出当前专注会话"，结果**整个托盘程序都退出了**
- 这是**破坏性操作**，必须二次确认 + 明确告知"将完全退出 Task Verge"
- **现状**：`quitApp` 点击已加 `confirmDlg`，明确告知"点击确定将完全退出 Task Verge 程序（包括托盘）"，未完成任务还会显示剩余数量

**问题 #21：专注时段设置无即时反馈** ✅ 已修复
- 设置 09:00-18:00 后，UI 不显示"当前是否在专注时段"
- 实际后端有 `in_work_window` 逻辑，但**移除守护进程后这个设置已经没有任何效果**（没有拦截功能了）
- **这个设置是"摆设"**，应该移除或重新赋予功能
- **现状**：设置页已移除工作时段编辑入口；`schedule` 字段仍保留在配置中但不影响任何行为

---

### 3.5 复盘页（Review）

**问题 #22：复盘页信息密度低且无 actionable** ❌ 未修复
- 三个面板：退出记录、事件流、每日归档
- 事件流是技术日志（"AI 自动识别应用"、"plan_lock"），**对用户无意义**
- 用户想看的是"我这周完成了多少、专注度趋势、常分心到哪些应用"
- 当前只有"每日归档"列表，**没有汇总统计、没有图表、没有洞察**
- **现状**：review 页面仍只有 3 个静态面板（退出记录 / 事件流 / 每日归档）

**问题 #23：归档记录不可点击查看详情** ❌ 未修复
- 每日归档只显示"目标 · 完成 2/3 · 67%"
- 想看那天的具体任务、前台时间分布？**点不开**
- **现状**：`renderReview()` 仍只渲染纯文本列表，归档项无点击交互

---

## 四、全局交互与视觉问题

### 4.1 导航与状态

**问题 #24：刷新按钮作用不明** ❌ 未修复
- 顶部"刷新"按钮 = 重新拉取 state
- 但总览页每 2 秒自动 `refreshLive`，**手动刷新冗余**
- 其他页（任务、规则、设置）不自动刷新，点刷新才有数据 —— 不一致
- **现状**：刷新按钮仍存在，自动刷新策略未变

**问题 #25：页面切换无记忆** ❌ 未修复
- 切换页面后状态保留，但**滚动位置丢失**
- 规则页滚到底部编辑分类，切到总览再回来，又回到顶部
- **现状**：无 `scrollTop` / `scrollTo` 相关逻辑

### 4.2 反馈机制

**问题 #26：toast 反馈转瞬即逝** ✅ 已修复
- `toast()` 只是改 `#statusPill` 文字，**没有动画、没有时间限制**
- 操作成功后状态 pill 一直显示"已保存"，直到下次操作才变
- 用户不知道是"刚刚成功"还是"历史状态"
- **现状**：`toast()` 已加 `toast-pulse` 动画类（重排触发）+ 4 秒超时复位为"就绪"/"休息中"

**问题 #27：错误弹窗已有但场景不全** ⚠️ 部分修复
- ✅ API 失败会弹 modal（已修复）
- ❌ 但 `prompt()` 弹窗（编辑任务、锁定原因）失败时只是 toast
- ❌ AI 生成任务失败走 fallback，**用户不知道失败了**
- **现状**：AI fallback 已加模态提示（`run('generate')` 检查 `fallback` 关键字）；但 `prompt()` 仍用于编辑任务/锁定原因/新增任务/解锁（4 处）

### 4.3 视觉一致性

**问题 #28：按钮样式不统一** ✅ 已修复
- 顶部"评估进度"是 primary（渐变）
- "生成任务"、"刷新"是普通按钮
- 任务页"保存任务"是 primary
- 规则页"保存分类"是普通按钮（**破坏性操作"自动分类"也是普通按钮**）
- **没有视觉优先级层次**
- **现状**：已建立三级按钮体系 — `.primary`（渐变蓝）、默认、`.danger`（红框）；"自动分类"和"清除前台时间数据"已用 `.danger`

**问题 #29：响应式断点过少** ✅ 已修复
- 只有 `@media(max-width:820px)` 一个断点
- 在 820-1200px 之间，hero、grid 布局拥挤
- 在 < 820px 时导航变图标但**没有 tooltip**，用户不知道图标代表什么
- **现状**：已增加 4 档断点（1600/1280/1024/820）；所有 `.nav-item` 均带 `title` 属性提供 tooltip

---

## 五、数据与隐私问题

**问题 #30：前台时间监控无告知** ✅ 已修复
- 后台线程每 2 秒采集一次前台窗口标题，存到 `fgtime.json`
- **用户不知道自己在被监控**，没有隐私提示
- `fg_title()` 会捕获窗口标题（可能包含敏感信息如邮件主题、文档名）
- **现状**：已实现 `showPrivacyNotice()`，首次使用时弹确认框告知"本应用会每 2 秒采集一次前台窗口标题"，用户确认后写入 `localStorage('taskVergePrivacyAck','v1')` 不再重复

**问题 #31：数据无导出/清除入口** ⚠️ 部分修复
- 用户想导出历史数据？没有
- 用户想清空所有数据重新开始？只能手动删 json 文件
- 复盘页的"归档今日"只是把当天数据存档，**不能删除归档**
- **现状**：设置页已加"清除前台时间数据"按钮（调用 `/api/clear-fg` 清空 `fgtime.json`）；但仍无导出入口、无归档删除

---

## 六、可靠性问题

**问题 #32：单实例锁机制脆弱** ✅ 已修复
- `claimFrontend()` 用 localStorage + 5 秒心跳
- 但**后端不限制**，用户可以同时开多个浏览器/标签页
- 多标签页同时操作会**并发写 task-config.json**，可能数据错乱
- **现状**：后端已加 X-Session token 机制（TTL 8 秒），写操作（除 quit/deepseek-key/upload-evidence/event 外）均需 token 校验，冲突时返回 409，前端 `api()` 自动弹"会话冲突"模态

**问题 #33：服务端口随机 + 进程退出后无重启** ❌ 未修复
- 端口写在 `task-panel.url`，用户收藏夹里的 URL 会失效
- 程序崩溃后**不会自动重启**，托盘也消失，用户不知道怎么办
- 之前的"守护进程"本该解决这个，已被移除
- **现状**：端口仍随机分配；崩溃后仍无自启机制

**问题 #34：DeepSeek Key 配置入口隐蔽** ✅ 已修复
- Key 从 `.env` 文件读，**UI 完全没有配置入口**
- 普通用户不知道怎么配，文档也少
- 建议 UI 提供配置框（哪怕只是写文件）
- **现状**：设置页已加 Key 输入框（`#deepseekKey`）+ "保存 Key"按钮，调用 `/api/deepseek-key` 写入 `.env`

---

## 七、优先级排序与改进建议

### 🔴 P0 - 影响核心体验，必须修

| # | 状态 | 问题 | 建议 |
|---|------|------|------|
| 1 | ❌ | 产品定位与功能不符（守护已移除） | 要么恢复专注控制能力，要么改名"任务伙伴"等更贴合的名字 |
| 2 | ✅ | 首次使用无引导 | 增加空状态引导："第一步：去设置页填目标" |
| 3 | ✅ | 申请退出直接杀进程 | 二次确认 + 明确告知"将退出整个程序" |
| 4 | ✅ | 批量编辑清空 done_flags | 修复：textarea 输入时保留已有 done_flags |
| 5 | ✅ | 自动分类无确认 | 弹确认框，警告会清空当前分类 |
| 6 | ✅ | 专注时段设置是摆设 | 移除或恢复拦截功能 |

### 🟡 P1 - 影响易用性，应该修

| # | 状态 | 问题 | 建议 |
|---|------|------|------|
| 7 | ⚠️ | 设置页混入日常操作（休息、退出） | 休息入口提到总览页快捷区 |
| 8 | ✅ | 规则页内容单薄 | 改名"应用分类"或补充真规则 |
| 9 | ✅ | 前台时间显示 exe 名 | 转成应用友好名 |
| 10 | ✅ | 休息次数不显示今日用量 | UI 显示"今日已休息 1/3 次" |
| 11 | ✅ | AI 识别应用无标记 | 桌面图标加"AI/手动"角标 |
| 12 | ⚠️ | 移除应用入口太深 | 改为 hover 显示 × 按钮 |
| 13 | ✅ | DeepSeek Key 无 UI 配置 | 设置页增加 Key 输入框 |
| 14 | ✅ | 生成任务降级无提示 | fallback 时 toast 明确"未配置 Key，已用本地模板" |

### 🟢 P2 - 体验优化，可以修

| # | 状态 | 问题 | 建议 |
|---|------|------|------|
| 15 | ❌ | 复盘页无汇总统计 | 增加周/月汇总图表 |
| 16 | ❌ | 归档不可点击查看详情 | 点击展开当日任务详情 |
| 17 | ✅ | toast 无动画/超时 | 加 fade-in/out，3 秒后恢复"就绪" |
| 18 | ✅ | 历史趋势空状态无提示 | `<2 天时显示"暂无足够数据"` |
| 19 | ❌ | 分类不可增删类别 | 允许用户添加/重命名/删除类别 |
| 20 | ✅ | 按钮样式无优先级 | 统一 primary/secondary/danger 三级 |
| 21 | ⚠️ | 数据无导出 | 增加"导出 JSON"按钮（已加"清除前台时间"） |

### 🔵 P3 - 锦上添花

| # | 状态 | 问题 | 建议 |
|---|------|------|------|
| 22 | ✅ | 前台时间监控无隐私提示 | 首次启动弹窗告知 |
| 23 | ✅ | 单实例锁脆弱 | 后端增加 token 校验 |
| 24 | ❌ | 崩溃无自启 | 可选：注册 Windows 计划任务 |
| 25 | ✅ | 响应式断点少 | 增加 1024px、1280px 断点 |

---

## 八、用户旅程走查（典型场景）

> 以下场景为 2026-07-05 评审时的状态，每步标注 2026-07-06 核对结果。

### 场景 A：新用户首次使用

1. 双击打开 → 浏览器弹出 → **看到"尚未设置目标"，懵** ❌ → ✅ 已加 onboarding 横幅分步引导
2. 点"生成任务" → toast"已开始生成任务" → 状态显示"完成" → **任务列表还是空** ❌ → ✅ fallback 时弹模态提示去配 Key
3. 不知道要去设置页填目标 → **流失** ❌ → ✅ 横幅中"设置"可点击跳转

### 场景 B：老用户日常使用

1. 打开 → 看到昨天的任务和进度 ✓
2. 想休息 5 分钟 → 设置页 → 只有 10/20/30 分钟 → **被迫选 10 分钟** ❌ → ✅ 已有 5 分钟档 + dashboard 快捷入口
3. 休息中状态显示"休息中" ✓ 但**不知道还剩多久** ❌ → ✅ `renderBreakWidget` 已显示"休息中，剩 X 分钟（今日 X/3 次）"
4. 想加一个新应用到桌面 → 申请增加 → AI 判断"不需要" → **挫败** ❌ → ❌ 仍由 AI 评判（问题 #4 未修）

### 场景 C：周末复盘

1. 打开复盘页 → 看到一堆"plan_lock"、"ai_apps"事件 → **看不懂** ❌ → ❌ 仍为技术日志
2. 想看周三的任务详情 → 点归档 → **点不开** ❌ → ❌ 仍不可点击
3. 想看本周专注度趋势 → **没有图表** ❌ → ❌ 仍无汇总图表
4. 放弃复盘 → **流失** ❌

---

## 九、总结

### 9.1 当前状态评分（用户视角）

> 以下评分为 2026-07-05 评审时打分；最后一列为 2026-07-06 核对后的调整。

| 维度 | 原评分 | 现评分 | 说明 |
|------|--------|--------|------|
| 功能完整性 | 4/10 | 4/10 | 核心专注控制仍缺失，只剩任务+统计 |
| 易用性 | 3/10 | 6/10 | 已加引导、休息快捷入口、应用友好名、AI/手角标 |
| 视觉设计 | 6/10 | 7/10 | 已加三级按钮体系、4 档响应式断点、nav tooltip |
| 反馈机制 | 5/10 | 7/10 | toast 加动画+超时、fallback 弹模态、退出二次确认 |
| 可靠性 | 5/10 | 7/10 | 已加 X-Session 后端锁，但崩溃仍无自启 |
| 复盘价值 | 2/10 | 2/10 | 复盘页仍无改进 |

**原综合评分：4.2/10** → **现综合评分：5.5/10** —— P0/P1 大部分已修，P2 复盘价值仍待提升。

### 9.2 核心建议（按当前状态更新）

1. **明确产品定位**：要么恢复"专注控制"能力，要么调整定位为"AI 任务伙伴"（❌ 未做）
2. ~~**补全新手引导**~~：✅ 已加 onboarding 横幅
3. ~~**修复数据安全**~~：✅ done_flags 保留、自动分类确认、退出二次确认均已修
4. ~~**重组信息架构**~~：⚠️ 休息已提到 dashboard，退出仍在设置页（可接受）
5. **增强复盘价值**：增加汇总统计、可点击归档、趋势图表（❌ 未做，仍为核心短板）
6. **替换 prompt()**：4 处 prompt() 仍用于编辑任务/锁定原因/新增/解锁，应统一改为 inline 或 modal（❌ 未做）

> 当前产品更像"开发者原型"而非"用户产品"，需要从功能驱动转向场景驱动重新打磨。
